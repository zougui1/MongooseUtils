# installation

install npm dependencies
> npm i

# run the project

compile typescript into javascript
> npm run compile

**note**: it automatically watch for changes

run the project
> npm run start

**note**: it automatically watch for changes
